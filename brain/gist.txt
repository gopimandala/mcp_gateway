cd /home/ubuntu/mcp_gateway && uv run -m src.main_http_wrapper
cd /home/ubuntu/mcp_gateway/kong_gw
docker ps

cd /home/ubuntu/mcp_gateway/brain 
uv run guardrail_server.py
uv run brain_server.py
(or) uv run uvicorn brain_server:app --host 0.0.0.0 --port 9000 --reload

curl -X POST http://localhost:9000/run_brain -H "Content-Type: application/json" -d '{"user_request":"are you stupid"}'

# Streamlit UI
cd /home/ubuntu/mcp_gateway/chat
uv run streamlit run brain_chat.py